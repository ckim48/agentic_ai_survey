\caption{AAI-CDOS-assisted real-time vehicular digital-twin (DT) synchronization over a space--air--ground integrated network. A structured service intent is decomposed into domain observations and proposals from the O-RAN, core-network, computing, and E2E service agents. The displayed GPT-4o example is the sanitized audited event \texttt{n10\_s2026\_e1}, in which the verifier accepts plan \texttt{p0} (ground access and \texttt{ground1} processing) with a predicted synchronization latency of 139.2~ms under a 1000-ms hard deadline. The two result plots report a separate 30-seed policy-level planner-surrogate evaluation and show mean values with 95\% confidence intervals; they must not be interpreted as GPT-4o API latency or as performance generated by the single audited event.}
